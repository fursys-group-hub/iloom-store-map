# 🛡️ 배포 전 검토 결과

보안 점검과 배포 준비 두 가지를 함께 확인했어요. 아래에서 차례대로 보시면 됩니다.

- **보안:** 통과 ✅
- **배포 준비:** 완료 ✅

> ✅ 배포 가능합니다.

- **대상 폴더:** `C:\Users\HP\iloom-store-map`
- **코드 저장소:** fursys-group-hub/iloom-store-map
- **프로젝트 종류:** 정적 웹페이지(HTML) + 데이터 수집 스크립트
- **검사 일시:** 2026-06-30 16:16

**발견 요약:** 치명 0 · 높음 0 · 중간 0 · 낮음 0

---

## 🔒 보안 점검

코드와 기록에서 발견한 보안 문제예요. 치명·높음은 고친 뒤 다시 검토해주세요.

| 심각도 | 위치 | 유형 | 설명 |
|---|---|---|---|
| ➖ | - | - | 발견된 보안 문제가 없어요. (화면에 들어 있는 카카오 지도 키는 지도를 띄우려면 반드시 화면에 포함돼야 하는 공개용 키라 정상입니다.) |



---

## 🚀 배포 준비

사내 서버에 올릴 수 있는 상태인지 확인했어요. ✅ 통과 · ❌ 문제 · ➖ 권장입니다.

| 점검 항목 | 결과 | 설명 |
|---|---|---|
| Dockerfile(도커 설정) | ✅ | 사내 서버에 올릴 도커 설정 파일이 있어요(정적 웹서버 nginx). |
| 접속 포트 일치 | ✅ | 도커 설정 포트(80)와 웹서버가 실제로 쓰는 포트가 같아요. |
| 시작 방법(켜질 때 실행) | ✅ | 컨테이너가 켜지면 nginx 웹서버가 자동으로 실행돼요. |
| 꼭 필요한 설정값 | ✅ | 이 페이지는 따로 입력해야 켜지는 설정값이 없어요(그대로 열면 동작). |
| 상태 점검(HEALTHCHECK) | ✅ | 배포 후 정상 동작을 확인하는 상태 점검이 들어 있어요. |



---

## ⚙️ 설정값 정리

이 앱이 쓰는 설정값과 다루는 방법이에요.

| 설정값 이름 | 다루는 방법 | 설명 |
|---|---|---|
| `GITHUB_OUTPUT` | 서버에서만 쓰는 일반 값 | GitHub Actions(데이터 수집 자동화)에서만 쓰는 값이라, 웹 배포와는 관계가 없어요. |

---

*배포 전 검토 · fursys-deploy-hub · 퍼시스홀딩스 IT본부 AI추진팀*
